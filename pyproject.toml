[project]
name = "calorie-tracker"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "anthropic",
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "pydantic",
    "psycopg2-binary",
    "passlib[bcrypt]",
]
